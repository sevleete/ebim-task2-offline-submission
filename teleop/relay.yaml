
sides:
  right: hold

raw_topic: /{side}/gello/raw/joint_states

out_topic: /{side}/gello/joint_states
dry_run_out_topic: /{side}/gello_relay_preview/joint_states

robot_joint_topic: /{side}/franka_robot_state_broadcaster/measured_joint_states

rate_hz: 100
watchdog_seconds: 0.3
max_engage_offset_rad: 1.0
align_done_rad: 0.08
go_blend_seconds: 0.5
